#include <iostream>

int main(int argc, char const *argv[]) {
    for (size_t x = 0; x < 100; x++) {
        std::cout << x << '\n';
      }
    /*for (size_t y = 100; y < 0; y--) {
        std::cout << y << '\n';
      z=x*y;
      std::cout << z << '\n';*/
    }
  return 0;
}
